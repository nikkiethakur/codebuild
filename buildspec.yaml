version: 0.2
 
phases:
  install:
    runtime-versions:
      nodejs: 16
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - aws s3 sync build/ s3://newtevio
      - aws cloudfront create-invalidation --distribution-id E1OMWX3FEIZLRZ --paths "/*"
artifacts:
  files: '**/*'
  base-directory: build
  discard-paths: yes
